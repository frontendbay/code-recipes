---
import Avatar from '../assets/avatar.png';


console.log(Avatar.src)
---

<aside class="not-content relative group border border-solid border-accent-200 p-4 pb-0 rounded-xl overflow-hidden">
	<div class="">
		<a href="https://starlight.astro.build/">
			<h2 class="text-lg">Want to collaborate on a project?</h2>
		</a>
		<p class="text-sm w-[60%] pb-4">
			Let's work together on your next big idea.<b>→</b>
		</p>
	</div>
	<div class="text-right absolute bottom-0 right-0">
		<img src={Avatar.src} alt="" width="120" height="auto" class="-mb-5 group-hover:translate-y-[-4px] transition-all duration-300 ease-in-out scale-x-[-1]"/>
	</div>
</aside>

<style>
	/* aside {
		margin: 1rem 0;
		position: relative;
		background-color: var(--sl-color-gray-6);
		padding: 1rem;
		border-radius: 0.5rem;
		display: flex;
		border: 1px solid var(--sl-color-text-accent);
		box-shadow: var(--sl-shadow-md);
		overflow-y: hidden;
	}
	aside a::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: 1;
	}
	aside h2 {
		font-size: var(--sl-text-lg) !important;
	}
	aside p {
		font-size: var(--sl-text-sm) !important;
		line-height: normal;
	}
	aside > div:last-child {
		display: flex;
		align-items: end;
	}
	aside img {
		margin-bottom: -1rem;
		transition: transform 0.3s;
		transform: translateY(2px);
		width: 5.5rem;
		height: auto;
	}

	aside:hover img {
		transform: translateY(0px);
	} */
</style>